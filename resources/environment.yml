name: fastapi-sample-dev
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.7
  # Core scientific python
  - numpy
  - matplotlib
  - fastapi
  - uvicorn
  - setuptools
  - pip:
    - .
